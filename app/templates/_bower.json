{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "requirejs": "~2.1.11"<% if (hasFeat) { %>,<% } if (includeModernizr) { %>
	"modernizr": "~2.6.2"<% if (includejQuery) {%>,<%}} else { if (includeHTML5Shiv) { %>
	"html5shiv": "~3.7.0"<% if (includejQuery) {%>,<%}}} if (includejQuery) { %>
	"jquery": "~2.1.0"<% } %>
  },
  "devDependencies": {},
  "ignore": [
    "app/scripts/vendor",
    "node_modules",
    "tests",
    "test",
    "**/.*"
  ]
}
